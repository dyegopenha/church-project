<p style="color: green"><%= notice %></p>
<div class="card">
  <h1 class="card-header">Document</h1>
  <div class="card-body">
    <%= render @document %>
    <div class="mt-3">
      <%= link_to "Back to documents", documents_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>