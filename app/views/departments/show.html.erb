<p style="color: green"><%= notice %></p>
<div class="card">
  <h1 class="card-header">Department</h1>
  <div class="card-body">
    <%= render @department %>
    <div>
      <%= link_to "Back to departments", departments_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
